
//area of circle by passing the value

#include<stdio.h>
int main()
float r,area;
float(pi=3.14) //formal parameter
{
    float radius,area;
    area= pi*r*r;
    return area;
}
void main()
{
      float r,area;
    printf("enter the radius");
    scanf("%d%f",&area,&r);
    area = pi*r*r;
    printf("\n area = %d\n",area);
}